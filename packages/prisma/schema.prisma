generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model KitOption {
  id                   String               @id @default(uuid())
  gameId               String
  displayName          String
  slotKey              String
}