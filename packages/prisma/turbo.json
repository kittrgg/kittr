{
  "extends": ["//"],
  "pipeline": {
    "db:generate": {
      "outputs": ["../../node_modules/.prisma/**"],
      "outputMode": "new-only"
    },
    "db:deploy": {
      "cache": false
    },
    "db:push": {
      "cache": false
    }
  }
}
