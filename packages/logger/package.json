{
	"name": "logger",
	"version": "0.0.0",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"source": "./src/index.ts",
	"private": true,
	"files": [
		"dist/**"
	],
	"scripts": {
		"test": "echo \"No logger tests have been written...yet.\"",
		"test:watch": "jest --watch --coverage",
		"dev": "tsc -w",
		"build": "tsc",
		"lint": "eslint src --fix",
		"clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
		"type-check": "tsc --pretty --noEmit"
	},
	"devDependencies": {
		"@types/jest": "^26.0.22",
		"scripts": "*",
		"jest": "^26.6.3",
		"eslint": "^7.32.0",
		"tsconfig": "*",
		"typescript": "^4.5.3"
	},
	"jest": {
		"preset": "scripts/jest/node"
	}
}
