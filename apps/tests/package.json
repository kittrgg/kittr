{
	"name": "tests",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"test:e2e": "cypress open --config-file ./config.dev.json",
		"clean": "rm -rf .turbo && rm -rf .next &&  rm -rf coverage && rm -rf node_modules"
	},
	"dependencies": {
		"@kittr/prisma": "workspace:*"
	},
	"devDependencies": {
		"cypress": "9.5.0",
		"typescript": "4.7.4"
	}
}
