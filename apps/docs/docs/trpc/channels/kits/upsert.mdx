# Upsert Kit to Channel

Create or update a kit for a channel

# Usage

```js
const { data: channel } = trpc.useQuery([
	"channels/games/kits/upsert",
	{ id: "123", kit: {} }
])
```

# Returns

This query returns the newly updated Channel object with the newly updated kit.

```json
 // TODO: Payload, please
```
