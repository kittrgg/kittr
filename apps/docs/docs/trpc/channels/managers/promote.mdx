# Promote a Manager

Promote a current manager of a channel from "EDITOR" to "ADMIN".

# Usage

Note: The managerId is the the id from the database, not Firebase.

```js
const { data: channel } = trpc.useQuery([
	"channels/managers/promote",
	{ channelId: "123", managerId: "456" }
])
```

# Returns

This query returns the newly updated Channel object with the newly promoted manager.

```json
 // TODO: Payload, please
```
