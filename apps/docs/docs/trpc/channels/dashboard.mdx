# Get Dashboard Channel

An endpoint specifically for getting a full channel for the dashboard to work.

# Usage

```js
const { data: dashboardChannel } = trpc.useQuery(["channels/dashboard"])
```

# Returns

This query returns the Channel object for the dashboard with all of it's needed relations.
This isn't the best programming Anthony has ever done.

```json
 // TODO: Payload, please
```
