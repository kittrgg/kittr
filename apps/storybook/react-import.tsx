/**
 * We want define react automatically for us when it builds tsx files
 * that don't specifically import React. This file is injected
 * into the build process.
 */
import React from "react"

export { React }
